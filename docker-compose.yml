# docker-compose.yml
services:
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.frontend
    container_name: askle-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.backend
    container_name: askle-backend
    ports:
      - "8000:8000"
    volumes:
      - ./:/app                       # Mount code for live reload if desired
      - ./data:/app/data             # Persist SQLite DB and FAISS files
    environment:
      # SQLite DB path inside container
      - SQL_DB_PATH=/app/data/papers.db

      # Vector DB paths (including file extensions as used in vector_database.py)
      - VECTOR_INDEX_PATH=/app/data/vector_index.faiss
      - VECTOR_METADATA_PATH=/app/data/vector_metadata.pkl

      # Sentence‑Transformer cache (optional)
      - HF_HOME=/app/data/

      # DeepSeek model path
      - DEEPSEEK_MODEL_PATH=/app/data/hub/models--deepseek-ai--deepseek-r1-distill-qwen-1.5b
    command: >
      sh -c "uvicorn app:app --host 0.0.0.0 --port 8000 --reload"

volumes:
  data:


